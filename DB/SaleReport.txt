CREATE TABLE SALEREPORT 
	(RECEIPTID DECIMAL(10), 
	PRODUCTID DECIMAL(10), 
	PURCHASEQUANTITY DECIMAL(10), 
	PRODUCTPRICE DECIMAL(10), 
	TOTALEACHPRODUCT DECIMAL(10), 
	TOTALPURCHASE DECIMAL(10), 
	PURCHASEDATE DATE);
